{
  "name": "simple-notify",
  "version": "0.1.0",
  "description": "opencode plugin for macOS session notifications",
  "main": "./dist/index.js",
  "type": "module",
  "files": [
    "src",
    "dist",
    "README.md",
    "AGENTS.md"
  ],
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bun build src/index.ts --target=node --outfile=dist/index.js",
    "prepare": "bun run build",
    "test": "bun test",
    "check": "bun run test && bun run build"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:ShuKe365/simple-notify.git"
  },
  "keywords": [
    "opencode",
    "plugin",
    "notify"
  ],
  "license": "MIT",
  "dependencies": {
    "@opencode-ai/plugin": "1.15.10"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
